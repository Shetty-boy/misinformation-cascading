# CASCADE2VEC — Minor_proj_lab
# Pinned dependencies for the whole team. Everyone runs:
#   python3 -m venv venv && source venv/bin/activate
#   pip install --upgrade pip
#   pip install -r requirements.txt
#
# Java 17 must be installed separately (not a pip package) — required by Spark.
# See the Team Setup Playbook, Part A, Step 1.

# --- Distributed data / graph processing ---
pyspark==3.5.1
graphframes-py==0.9.0
pyarrow==15.0.2

# NOTE on GraphFrames: the pip package above only installs the Python API.
# The actual Spark package (JVM side) is pulled at runtime with:
#   pyspark --packages graphframes:graphframes:0.8.3-spark3.5-s_2.12
# Add the same --packages flag to spark-submit / SparkSession config in code
# (see src/graph/build_graph.py — spark session is configured centrally there).

# --- Core data science ---
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.0
xgboost==2.0.3
statsmodels==0.14.2
networkx==3.3

# --- Deep learning / GNN ---
torch==2.3.1
torch-geometric==2.5.3

# --- Explainability ---
shap==0.45.1
lime==0.2.0.1

# --- Visualization ---
matplotlib==3.9.0
seaborn==0.13.2

# --- Experiment tracking ---
mlflow==2.14.1
pyyaml==6.0.1
tqdm==4.66.4

# --- Notebooks ---
jupyter==1.0.0
jupyterlab==4.2.2

# --- Dev tooling / testing ---
pytest==8.2.2
pytest-cov==5.0.0
black==24.4.2
flake8==7.0.0
pre-commit==3.7.1

# --- Document/report generation (paper figures, tables export) ---
python-docx==1.1.2
